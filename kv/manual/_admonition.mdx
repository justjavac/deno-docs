:::caution Deno KV 目前处于测试阶段

Deno KV 目前是 **实验性的**，**可能会发生变化**。尽管我们尽最大努力确保数据持久性，但数据丢失是可能的，特别是在 Deno 更新时。

执行使用 KV 的 Deno 程序目前需要使用 `--unstable` 标志，如下所示：

```sh
deno run -A --unstable my_kv_code.ts
```

:::
